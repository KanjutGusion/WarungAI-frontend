{
  "name": "WarungAI-frontend",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "start": "node .output/server/index.mjs",
    "clean": "rm -rf .nuxt .output node_modules/.cache",
    "rebuild": "npm run clean && npm run build",
    "prod": "npm run rebuild && npm run start"
  },
  "dependencies": {
    "nuxt": "^3.14.0",
    "vue": "^3.5.13",
    "vue-router": "^4.4.5"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^6.14.0",
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3"
  }
}
